## 1、逻辑回归

我们使用鸢尾花（Iris）数据集来演示逻辑回归。鸢尾花数据集包含150个样本，每个样本有4个特征，分别是花萼长度、花萼宽度、花瓣长度和花瓣宽度。目标变量是花的品种，共有三个品种：Setosa、Versicolour和Virginica。为了简化问题，我们将使用逻辑回归对是否为Setosa品种进行二分类。

[逻辑回归](code/逻辑回归.py ':include :type=code ')

## 2、多项式逻辑回归

在使用逻辑回归进行分类任务时，有时线性模型可能不足以捕捉特征之间的复杂关系。添加多项式特征可以帮助模型更好地拟合数据。以下是一个详细教程，演示如何使用 Python 和 scikit-learn 添加多项式特征到逻辑回归模型中。

[多项式逻辑回归](code/多项式逻辑回归.py ':include :type=code ')

3、OVO & OVR

[OVO & OVR](code/ovo&ovr.py ':include :type=code ')