## 1、从零实现一元线性回归

我们将使用了自定义的简单线性回归模型来对糖尿病数据集进行训练和预测，并计算了模型的评估指标。在这个例子中，模型预测的目标是糖尿病数据集中的一个特征。

[简易线性回归](code/1-从零实现.py ':include :type=code ')

## 2、使用`scikit-learn`实现一元线性回归

[scikit-learn-一元线性回归](code/2-sklearn实现.py ':include :type=code ')

**计算斜率 \( a \) 和截距 \( b \) 的向量化运算**

```python
self.a_ = np.sum((X - x_mean) * (y - y_mean)) / np.sum((X - x_mean) ** 2)
self.b_ = y_mean - self.a_ * x_mean
```

- `np.sum((X - x_mean) * (y - y_mean))`：计算分子部分，即$\sum_{i=1}^{m} (x^{(i)} - \bar{x})(y^{(i)} - \bar{y})$。
- `np.sum((X - x_mean) ** 2)`：计算分母部分，即 $\sum_{i=1}^{m} (x^{(i)} - \bar{x})^2$。
- `self.b_ = y_mean - self.a_ * x_mean`：根据公式计算截距。
