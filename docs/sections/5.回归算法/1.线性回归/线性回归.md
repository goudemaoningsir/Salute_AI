## 一、一元线性回归

### 1、介绍

一元线性回归是一种预测分析方法，其目标是确定两个变量之间的线性关系。在这种情况下，我们关注的是自变量  $x$  （解释变量或输入变量）和因变量  $y$ （响应变量或输出变量。）之间的关系。

以房屋价格预测为例，我们可以将房屋面积作为自变量  $x$ ，将房屋价格作为因变量  $y$。线性回归模型旨在找到一个线性方程，该方程能够最好地拟合数据点，即最小化预测值和实际值之间的差异。

![8](https://raw.githubusercontent.com/goudemaoningsir/Salute_AI/main/img/8.png)

最佳拟合的直线方程可以表示为：
$$
y = ax + b
$$
其中：
-  $a$  是斜率，表示自变量每变化一个单位，因变量预期的变化量。
-  $b$  是截距，表示当自变量为零时，因变量的预期值。

对于数据集中的每个样本点$x^{(i)}$，根据线性回归模型，其预测值 $ \hat{y}^{(i)} $ 可以计算为：
$$
\hat{y}^{(i)} = ax^{(i)} + b
$$
 $ \hat{y}^{(i)} $ 是给定房屋面积$x^{(i)}$时，根据模型预测的房屋价格，而 $ {y}^{(i)} $则为房屋的真值 。

### 2、损失函数

接下来，我们希望 $ \hat{y}^{(i)} $ 和$ {y}^{(i)} $的差距尽量小，因此定义以下损失函数，表达 $ \hat{y}^{(i)} $ 和$ {y}^{(i)} $的差距。
$$
（\hat{y}^{(i)} - {y}^{(i)})^2
$$
我们可以将每个样本点的损失函数相加，得到总的损失函数


$$
\sum_{i=1}^{m} (\hat{y}^{(i)} - y^{(i)})^2
$$
将 $\hat{y}^{(i)} = ax^{(i)} + b$ 代入上述公式得
$$
J(a,b) = \sum_{i=1}^{m} (ax^{(i)} + b - y^{(i)})^2
$$
在训练模型时，我们希望寻找一组参数（$\mathbf{a}^*, b^*$），这组参数能最小化在所有训练样本上的总损失。如下式：

$$
\mathbf{a}^*, b^* = \operatorname*{argmin}_{\mathbf{a}, b}\  J(\mathbf{a}, b).
$$

### 3、求解

最小二乘法（Least Squares Method）是用于拟合数据的一种标准方法。目标是找到参数 $a$ 和 $b$，使得损失函数（误差平方和）最小。下面是详细的推导过程：

#### （1）定义目标函数

给定数据点 $((x^{(i)}, y^{(i)})) (i = 1, 2, ..., m)$，我们的目标是找到线性函数 $\hat{y} = ax + b$，使得预测值 $\hat{y}$ 和实际值$y$之间的差距最小，即最小化以下损失函数（误差平方和）：

$$
J(a,b) = \sum_{i=1}^{m} (ax^{(i)} + b - y^{(i)})^2
$$

#### （2）求导数并设为零

要找到使损失函数最小的$a$和$b$，我们需要对 $a$和 $b$分别求偏导数，并设这些偏导数为零。

##### 1）对$b$求偏导数

$$
\frac{\partial J(a,b)}{\partial b} = \frac{\partial}{\partial b} \sum_{i=1}^{m} (ax^{(i)} + b - y^{(i)})^2
$$

同样使用链式法则：

$$
= \sum_{i=1}^{m} 2(ax^{(i)} + b - y^{(i)})*1
$$

将其设为零：

$$
\sum_{i=1}^{m} 2(ax^{(i)} + b - y^{(i)}) = 0
$$

可以简化为：

$$
\sum_{i=1}^{m} (ax^{(i)} + b - y^{(i)}) = 0 \quad
$$

拆分： 

$$
a \sum_{i=1}^{m} x^{(i)} + b \sum_{i=1}^{m} 1 - \sum_{i=1}^{m} y^{(i)} = 0
$$

将$bm$项移到右边得：

$$
a \sum_{i=1}^{m} x^{(i)}  - \sum_{i=1}^{m} y^{(i)} = -bm
$$

$\frac{\sum_{i=1}^{m} x^{(i)}}{m}$为$x$的均值$\bar{x}$，$\frac{\sum_{i=1}^{m} y^{(i)}}{m}$为$y$的均值$\bar{y}$，因此： 

$$
b=\bar{y} - a\bar{x}
$$

##### 2）对$a$求偏导数

$$
\frac{\partial J(a,b)}{\partial a} = \frac{\partial}{\partial a} \sum_{i=1}^{m} (ax^{(i)} + b - y^{(i)})^2
$$

使用链式法则：

$$
= \sum_{i=1}^{m} 2(ax^{(i)} + b - y^{(i)}) \cdot x^{(i)}
$$

将其设为零：

$$
\sum_{i=1}^{m} 2(ax^{(i)} + b - y^{(i)}) \cdot x^{(i)} = 0
$$

可以简化为：

$$
\sum_{i=1}^{m} (ax^{(i)} + b - y^{(i)}) \cdot x^{(i)} = 0 \quad
$$

##### 3）将$b$代入对$a$的偏导数方程

我们已经得到了$b = \bar{y} - a\bar{x}$，现在将其代入：

$$
\sum_{i=1}^{m} (ax^{(i)} + (\bar{y} - a\bar{x}) - y^{(i)}) \cdot x^{(i)} = 0
$$

展开括号：

$$
\sum_{i=1}^{m} (ax^{(i)} + \bar{y} - a\bar{x} - y^{(i)}) \cdot x^{(i)} = 0
$$

拆分求和：

$$
a \sum_{i=1}^{m} (x^{(i)} \cdot x^{(i)}) + \bar{y} \sum_{i=1}^{m} x^{(i)} - a\bar{x} \sum_{i=1}^{m} x^{(i)} - \sum_{i=1}^{m} (y^{(i)} \cdot x^{(i)}) = 0
$$

将$\sum_{i=1}^{m} x^{(i)}$表示为$m \bar{x}$：

$$
a \sum_{i=1}^{m} (x^{(i)})^2 + \bar{y} m \bar{x} - a\bar{x} m \bar{x} - \sum_{i=1}^{m} (y^{(i)} x^{(i)}) = 0
$$

合并 $a$ 项：

$$
a (\sum_{i=1}^{m} (x^{(i)})^2 - m \bar{x}^2) + m \bar{y} \bar{x} - \sum_{i=1}^{m} (y^{(i)} x^{(i)}) = 0
$$

得出

$$

$$

我们从以下方程开始：

$$
a \left(\sum_{i=1}^{m} (x^{(i)})^2 - m \bar{x}^2\right) + m \bar{y} \bar{x} - \sum_{i=1}^{m} (y^{(i)} x^{(i)}) = 0
$$

将$m \bar{y} \bar{x}$和$\sum_{i=1}^{m} (y^{(i)} x^{(i)})$移到右边：

$$
a \left(\sum_{i=1}^{m} (x^{(i)})^2 - m \bar{x}^2\right) = \sum_{i=1}^{m} (y^{(i)} x^{(i)}) - m \bar{y} \bar{x}
$$

因此，参数 $a$ 为：

$$
a = \frac{\sum_{i=1}^{m} (x^{(i)} - \bar{x})(y^{(i)} - \bar{y})}{\sum_{i=1}^{m} (x^{(i)} - \bar{x})^2}
$$



### 4、实现

下面是一个简单的一元线性回归的演示，包括生成数据、训练模型、预测并绘制回归结果。

[实现](code/LinearRegression_1.py ':include :type=code ')

## 二、多元线性回归

### 1、介绍

多元线性回归是线性回归的一种扩展，用于研究多个自变量（特征）对一个因变量（目标变量）的影响。与一元线性回归（只有一个自变量）不同，多元线性回归能够处理多个自变量，因此更适合于复杂的数据集。

多元线性回归的目标是找到最佳的线性组合来预测目标变量，公式为：

$$
\hat{y}^{(i)} = \beta_0 + \beta_1 x_1^{(i)} + \beta_2 x_2^{(i)} + \cdots + \beta_p x_p^{(i)}
$$

其中：

- $\hat{y}^{(i)}$ 是预测值。
- $\beta_0$ 是截距。
- $\beta_1, \beta_2, \ldots, \beta_p$ 是回归系数，表示每个自变量对目标变量的影响。
- $x_1^{(i)}, x_2^{(i)}, \ldots, x_p^{(i)}$是多个自变量。

进一步可以用矩阵形式表示多元线性回归：

$$
\hat{\mathbf{y}} =  \beta_0 + \mathbf{X} \boldsymbol{\beta}
$$

其中：

- $\mathbf{X}$ 是$n \times p$ 的设计矩阵，其余列为自变量数据：
  $$
  \mathbf{X} = \begin{pmatrix}
  x_1^{(1)} & x_2^{(1)} & \cdots & x_p^{(1)} \\
  x_1^{(2)} & x_2^{(2)} & \cdots & x_p^{(2)} \\
  \vdots & \vdots & \ddots & \vdots \\
  x_1^{(n)} & x_2^{(n)} & \cdots & x_p^{(n)}
  \end{pmatrix}
  $$

- $\boldsymbol{\beta}$ 是 $(p + 1) \times 1$的回归系数向量：
  $$
  \boldsymbol{\beta} = \begin{pmatrix}
  \beta_0 \\
  \beta_1 \\
  \beta_2 \\
  \vdots \\
  \beta_p
  \end{pmatrix}
  $$

- $\hat{\mathbf{y}}$ 是$n \times 1$ 的预测值向量：

  $$
  \hat{\mathbf{y}} = \begin{pmatrix}
  \hat{y}^{(1)} \\
  \hat{y}^{(2)} \\
  \vdots \\
  \hat{y}^{(n)}
  \end{pmatrix}
  $$

为了将截距项 $\beta_0$ 包含在矩阵运算中，我们需要在设计矩阵 $\mathbf{X}$ 中加一列全 1 的列，使得回归系数向量 $\boldsymbol{\beta}$ 中的第一个元素对应截距项 $\beta_0$。这样，我们可以统一用矩阵乘法来表示回归方程，而不需要单独处理截距项。

具体来说，设计矩阵 $\mathbf{X}$ 的结构如下，此时维度变为 $n \times (p + 1)$：
$$
\mathbf{X} = \begin{pmatrix}
x_0^{(1)} & x_1^{(1)} & x_2^{(1)} & \cdots & x_p^{(1)} \\
x_0^{(2)} & x_1^{(2)} & x_2^{(2)} & \cdots & x_p^{(2)} \\
\vdots & \vdots & \vdots & \ddots & \vdots \\
x_0^{(n)} & x_1^{(n)} & x_2^{(n)} & \cdots & x_p^{(n)}
\end{pmatrix}
$$

其中 $x_0^{(i)}$ 恒等于1，也就是第一列全是 1，表示每个样本的截距项，剩下的每列对应一个自变量的数据。
$$
\mathbf{X} = \begin{pmatrix}
1 & x_1^{(1)} & x_2^{(1)} & \cdots & x_p^{(1)} \\
1 & x_1^{(2)} & x_2^{(2)} & \cdots & x_p^{(2)} \\
\vdots & \vdots & \vdots & \ddots & \vdots \\
1 & x_1^{(n)} & x_2^{(n)} & \cdots & x_p^{(n)}
\end{pmatrix}
$$

通过这种方式，我们可以将截距项 $\beta_0$ 包含在矩阵运算中，从而简化计算过程。每一个预测值 $\hat{y}^{(i)}$ 都可以写成：
$$
\hat{y}^{(i)} = 1 \cdot \beta_0 + x_1^{(i)} \cdot \beta_1 + x_2^{(i)} \cdot \beta_2 + \cdots + x_p^{(i)} \cdot \beta_p= \mathbf{X} \boldsymbol{\beta}
$$

### 2、损失函数

在多元线性回归中，损失函数（Loss Function）用于衡量模型预测值与真实值之间的差异。最常用的损失函数是均方误差（Mean Squared Error，MSE），其公式如下：

$$
\text{MSE} = \frac{1}{n} \sum_{i=1}^{n} (y^{(i)} - \hat{y}^{(i)})^2
$$

其中：

- $n$ 是样本的数量。
- $y^{(i)}$ 是第 $i$ 个样本的真实值。
- $\hat{y}^{(i)}$ 是第 $i$ 个样本的预测值。

为了便于矩阵运算，可以将上述公式改写成矩阵形式。设：

- $\mathbf{y}$ 是 $n \times 1$ 的真实值向量：
  $$
  \mathbf{y} = \begin{pmatrix}
  y^{(1)} \\
  y^{(2)} \\
  \vdots \\
  y^{(n)}
  \end{pmatrix}
  $$

- $\hat{y}^{(i)}$是 $n \times 1$ 的预测值向量，其中 $\mathbf{X}$ 是 $n \times p$ 的特征矩阵，$\boldsymbol{\beta}$ 是 $p \times 1$ 的回归系数向量：
  $$
  \hat{y}^{(i)} = \mathbf{X} \boldsymbol{\beta}
  $$

将预测值 $\hat{y}^{(i)}$ 用矩阵形式表示为 $\mathbf{X}\boldsymbol{\beta}$：

$$
\text{MSE} = \frac{1}{n} \sum_{i=1}^{n} (\mathbf{y} - \mathbf{X}\boldsymbol{\beta})^2
$$

- $(\mathbf{y} - \mathbf{X} \boldsymbol{\beta})$ 是一个 $n \times 1$ 的列向量。
- $(\mathbf{y} - \mathbf{X} \boldsymbol{\beta})^T$ 是一个 $1 \times n$ 的行向量。

它们的乘积是一个标量，等于所有误差平方和。进一步的均方误差可以写成：
$$
\text{MSE} = \frac{1}{n} (\mathbf{y} - \mathbf{X} \boldsymbol{\beta})^T (\mathbf{y} - \mathbf{X} \boldsymbol{\beta})
$$

### 3、求解

为了最小化损失函数，需要求解 $\boldsymbol{\beta}$ 使得 MSE 最小。这可以通过求解梯度为零的方程来实现。首先，我们需要计算 MSE 对 $\boldsymbol{\beta}$ 的梯度：
$$
\nabla_{\boldsymbol{\beta}} \text{MSE} = \nabla_{\boldsymbol{\beta}} \left(\frac{1}{n} (\mathbf{y} - \mathbf{X} \boldsymbol{\beta})^T (\mathbf{y} - \mathbf{X} \boldsymbol{\beta})\right)
$$

首先，根据 $(\mathbf{a} - \mathbf{b})^T (\mathbf{a} - \mathbf{b}) = \mathbf{a}^T \mathbf{a} - 2 \mathbf{a}^T \mathbf{b} + \mathbf{b}^T \mathbf{b}$，我们展开 $\text{MSE}$：

$$
\frac{1}{n} (\mathbf{y} - \mathbf{X} \boldsymbol{\beta})^T (\mathbf{y} - \mathbf{X} \boldsymbol{\beta}) = \frac{1}{n} (\mathbf{y}^T \mathbf{y} - 2 \mathbf{y}^T \mathbf{X} \boldsymbol{\beta} + \boldsymbol{\beta}^T \mathbf{X}^T \mathbf{X} \boldsymbol{\beta})
$$

我们需要计算上式对 $\boldsymbol{\beta}$ 的梯度。记住，$\mathbf{y}^T \mathbf{y}$ 是常数项，与 $\boldsymbol{\beta}$ 无关，梯度为零。

针对 $-2 \mathbf{y}^T \mathbf{X} \boldsymbol{\beta}$：
$$
\nabla_{\boldsymbol{\beta}} (-2 \mathbf{y}^T \mathbf{X} \boldsymbol{\beta}) = -2 \mathbf{X}^T \mathbf{y}
$$

针对 $\boldsymbol{\beta}^T \mathbf{X}^T \mathbf{X} \boldsymbol{\beta}$：
$$
\nabla_{\boldsymbol{\beta}} (\boldsymbol{\beta}^T \mathbf{X}^T \mathbf{X} \boldsymbol{\beta}) = 2 \mathbf{X}^T \mathbf{X} \boldsymbol{\beta}
$$

结合上述两部分，我们得到梯度：
$$
\nabla_{\boldsymbol{\beta}} \text{MSE} = \frac{1}{n} (-2 \mathbf{X}^T \mathbf{y} + 2 \mathbf{X}^T \mathbf{X} \boldsymbol{\beta})
$$

将梯度化简为：
$$
\nabla_{\boldsymbol{\beta}} \text{MSE} = \frac{2}{n} (\mathbf{X}^T \mathbf{X} \boldsymbol{\beta} - \mathbf{X}^T \mathbf{y})
$$

我们可以将上式写成：
$$
\nabla_{\boldsymbol{\beta}} \text{MSE} = -\frac{2}{n} \mathbf{X}^T (\mathbf{y} - \mathbf{X} \boldsymbol{\beta})
$$

这就是我们对 $\boldsymbol{\beta}$ 求导的结果。将梯度设为零，得到：
$$
\mathbf{X}^T (\mathbf{y} - \mathbf{X} \boldsymbol{\beta}) = 0
$$

进一步整理得到：

$$
\mathbf{X}^T \mathbf{y} = \mathbf{X}^T \mathbf{X} \boldsymbol{\beta}
$$

即：

$$
\boldsymbol{\beta} = (\mathbf{X}^T \mathbf{X})^{-1} \mathbf{X}^T \mathbf{y}
$$

其中：

- $\mathbf{X}$ 是 $n \times (p+1)$ 的设计矩阵。
- $\boldsymbol{\beta}$ 是 $(p+1) \times 1$ 的回归系数向量。
- $\mathbf{X}^T$ 是设计矩阵 $\mathbf{X}$ 的转置矩阵。
- $\mathbf{X}^T \mathbf{X}$ 是 $ (p+1) \times (p+1) $ 的矩阵，且必须是可逆的。
- $(\mathbf{X}^T \mathbf{X})^{-1}$ 是逆矩阵。
- $\mathbf{y}$ 是 $n \times 1$ 的实际值向量。

这就是多元线性回归的闭式解（Closed-form Solution）。通过求解这个方程，我们可以得到最佳的回归系数 $\boldsymbol{\beta}$，使得模型的预测值尽可能接近真实值，从而最小化均方误差。

!>需要注意的是，在上述的闭式解计算中，涉及到矩阵求逆操作，时间复杂度比较高。矩阵求逆的时间复杂度通常是 $O(p^3)$，其中 $p$ 是特征的数量。当数据量非常大时，这种计算可能会变得不可行。

为了应对这种计算复杂度高的问题，可以使用迭代优化算法，如梯度下降（Gradient Descent）或随机梯度下降（Stochastic Gradient Descent, SGD），这些方法不需要直接求解矩阵逆，时间复杂度相对较低，更适合处理大规模数据集。

### 4、实现

首先，我们生成一个包含两个自变量 $x1$和 $x2$的数据集，以及目标变量$ y$，并展示数据集的关系。然后，使用生成的数据进行多元线性回归的训练，并展示模型评测结果。最后，绘制回归后的拟合结果图。

[实现](code/LinearRegression_2.py ':include :type=code ')

## 三、分段线性回归

### 1、确定函数

#### （1）简单线性模型

线性模型（Linear Model）是一种简单的模型，描述了输入和输出之间的线性关系。假设输入变量是$x_1$ ，输出变量是$y$ 。在线性模型中，它们的关系可以表示为一条直线：
$$
y = wx_1 + b
$$
其中，$w$ 是权重，决定了直线的斜率；$b$ 是偏置，决定了直线与 y 轴的交点。随着$x_1$ 的增加，$y$ 也会增加。这意味着不管如何调整$w$ 和$b$ ，这种关系永远都是线性的，即$x_1$ 越大，$y$ 越大。

#### （2）现实情况

然而，现实情况可能并不像线性模型那样简单：

- 可能在$x_1$ 小于某个数值时，$x_1$ 和$y$ 成正比。
- 当$x_1$ 大于某个数值时，可能会出现物极必反的现象，即$x_1$ 过大反而导致$y$ 减少。
- $x_1$ 和$y$ 之间可能存在更复杂的关系，如下图中的红色曲线所示。

![image-20210305112252045](https://raw.githubusercontent.com/goudemaoningsir/Salute_AI/main/img/11.png)

无论如何调整$w$ 和$b$ ，线性模型都无法模拟出这样的复杂关系。这种限制被称为模型偏差（Model Bias），不同于偏置项$b$ 的偏差（Bias）。

为了处理复杂的关系，我们需要更复杂且有弹性的函数。可以观察红色曲线，它可以视为一个常数项加上一组特定形状的函数，这些特定形状的函数有如下特性：

- 输入值小于某个阈值时，输出是定值。
- 输入值大于某个阈值时，输出是另一个定值。
- 中间有一个斜坡。

#### （3）组合函数生成复杂曲线

红色曲线可以视为常数项加上多个蓝色函数。要构造这样的曲线，可以按照以下步骤：

**Step1：**设置常数项，使得红色曲线与 x 轴的交点相同。

**Step2：**使用多个蓝色函数，每个函数在不同的区域内有不同的斜率，使得整体曲线形成所需的形状。

每个蓝色函数的起点和终点与红色曲线的转折点一致，叠加后逐步逼近红色曲线。

#### （4）用 Sigmoid 函数逼近蓝色函数

尽管直接写出蓝色函数可能不容易，但可以用 Sigmoid 函数来逼近它。Sigmoid 函数的形式为：
$$
y = c \cdot \frac{1}{1 + e^{-(b + wx_1)}}
$$
Sigmoid 函数有以下特性：

- 当$x_1$ 非常大时，$e^{-(b + wx_1)}$ 接近 0，$y$ 接近$c$。
- 当$x_1$ 非常小时，$e^{-(b + wx_1)}$ 很大，$y$ 接近 0。

因此，Sigmoid 函数呈现 S 型曲线，可以逼近蓝色函数。通过调整$w$、$b$ 和$c$ ，可以生成不同形状的 Sigmoid 函数：

- 改变$w$ 可以改变斜率。
- 改变$b$ 可以左右移动曲线。
- 改变$c$ 可以改变曲线的高度。

叠加多个不同形状的 Sigmoid 函数，可以逼近任何连续的复杂曲线。

![image-20210305132035492](https://raw.githubusercontent.com/goudemaoningsir/Salute_AI/main/img/10.png)

#### （5）最终函数形式

如果要表示红色曲线，最终函数形式为：
$$
y = b + \sum_i {c_i \cdot sigmoid(b_i + w_i x_1)}
$$
其中，每个$c_i \cdot sigmoid(b_i + w_i x_1)$ 表示一个蓝色函数，通过调整$c_i$、$b_i$ 和$w_i$ ，可以逼近红色曲线的各个部分。$b$、$b_i$ 和 $w_i$ 是我们未知的参数，通过不同的参数设定，可以生成不同的蓝色函数，从而生成不同的红色曲线。利用这些红色曲线的叠加，我们可以逼近各种连续函数。

![image-20210305134454023](https://raw.githubusercontent.com/goudemaoningsir/Salute_AI/main/img/12.png)

通过这样的组合，我们可以构建出逼近任意连续函数的模型。这种方法不仅克服了线性模型的局限，还提供了更大的灵活性来模拟复杂的现实关系。

我们可以扩展该函数以包含多个特征，用 $j$ 来表示特征的编号，例如：
$$
y = b + \sum_j w_j x_j
$$
通过将该式子嵌入到 Sigmoid 函数中，可以写成：
$$
y = b + \sum_i c_i \sigma(b_i + \sum_j w_{ij} x_j)
$$

如图所示：

![image-20210305144019248](https://raw.githubusercontent.com/goudemaoningsir/Salute_AI/main/img/13.png)

考虑三个特征的情况，即 $j = 1, 2, 3$，输入分别是 $x_1$、$x_2$ 和 $x_3$，每个 $i$ 表示一个蓝色的函数。

对于第一个 Sigmoid 函数，括号内的计算如下：
$$
b_1 + w_{11} x_1 + w_{12} x_2 + w_{13} x_3
$$

对于第二个 Sigmoid 函数：
$$
b_2 + w_{21} x_1 + w_{22} x_2 + w_{23} x_3
$$

对于第三个 Sigmoid 函数：
$$
b_3 + w_{31} x_1 + w_{32} x_2 + w_{33} x_3
$$

为了简化表示，我们用 $r_i$ 来表示括号内的计算结果：
$$
r_1 = b_1 + w_{11} x_1 + w_{12} x_2 + w_{13} x_3
$$

$$
r_2 = b_2 + w_{21} x_1 + w_{22} x_2 + w_{23} x_3
$$

$$
r_3 = b_3 + w_{31} x_1 + w_{32} x_2 + w_{33} x_3
$$

我们可以将向量形式表示为：
$$
\begin{bmatrix} r_1 \\ r_2 \\ r_3 \end{bmatrix} = \begin{bmatrix} b_1 \\ b_2 \\ b_3 \end{bmatrix} + \begin{bmatrix} w_{11} & w_{12} & w_{13} \\ w_{21} & w_{22} & w_{23} \\ w_{31} & w_{32} & w_{33} \end{bmatrix} \begin{bmatrix} x_1 \\ x_2 \\ x_3 \end{bmatrix}
$$
进一步表示为：
$$
r = b + W x
$$

通过 Sigmoid 函数得到向量 $a$：
$$
a = \sigma(r)
$$

再通过与向量 $c$ 的乘积和加法得到最终输出 $y$：
$$
y = b + c^T a
$$

![image-20210305150004077](https://raw.githubusercontent.com/goudemaoningsir/Salute_AI/main/img/14.png)

上述操作可以简化为：
$$
y = b + c^T \sigma( b + W x)
$$

### 2、定义损失函数

在定义损失函数时，为了简化处理，我们将所有模型参数统称为参数向量 $ θ $。该向量包含权重矩阵 $ W $、偏置向量 $ b $、输出层参数 $ c $ 以及输出层的偏置项 $ b' $。损失函数 $ L(θ) $ 用于衡量参数 $ θ $ 下模型的误差。

首先，给定参数 $ θ $ 的一组值（即特定的 $ W $、$ b $、$ c $ 和 $ b' $），然后将输入特征 $ x $ 代入模型，得到预测输出 $ y $。接着，计算预测输出 $ y $ 与真实标签 $ y_{\text{true}} $ 之间的误差 $ e $，并将所有样本的误差累加，得到总的损失值 $ L(θ) $。

损失函数的表达式为：

$$
L(θ) = \sum_{i=1}^N e_i = \sum_{i=1}^N (y_i - y_{\text{true}, i})^2 
$$
其中，$ N $ 是样本总数，$ y_i $ 是模型预测的第 $ i $ 个样本的输出，$ y_{\text{true}, i} $ 是第 $ i $ 个样本的真实标签。

### 3、优化过程

定义了损失函数后，我们的目标是找到使损失函数 $ L(θ) $ 最小的参数 $ θ $。为此，我们使用梯度下降法进行优化，具体步骤如下：

**Step1：初始化参数**。随机选择初始参数值 $ θ_0 $，记为列向量：
$$
θ_0 = \begin{pmatrix}
θ_{1,0} \\
θ_{2,0} \\
\vdots \\
θ_{M,0}
\end{pmatrix}
$$

**Step2：计算梯度**。计算当前参数 $ θ_k $ 下损失函数 $ L(θ) $ 的梯度 $ ∇L(θ_k) $。梯度 $ ∇L $ 是一个向量，其中包含了每个参数对损失函数的偏导数。在每次迭代中，计算当前参数 $ θ_k $ 下损失函数 $ L(θ_k) $ 的梯度 $ ∇L(θ_k) $，得到列向量：
$$
∇L(θ_k) = \begin{pmatrix}
\frac{\partial L(θ_k)}{\partial θ_{1,k}} \\
\frac{\partial L(θ_k)}{\partial θ_{2,k}} \\
\vdots \\
\frac{\partial L(θ_k)}{\partial θ_{M,k}}
\end{pmatrix}
$$
**Step3：更新参数**。根据梯度更新参数 $ θ_k $，得到新的参数 $ θ_{k+1} $：
$$
θ_{k+1} = \begin{pmatrix}
θ_{1,k+1} \\
θ_{2,k+1} \\
\vdots \\
θ_{M,k+1}
\end{pmatrix} = \begin{pmatrix}
θ_{1,k} \\
θ_{2,k} \\
\vdots \\
θ_{M,k}
\end{pmatrix} - η \begin{pmatrix}
\frac{\partial L(θ_k)}{\partial θ_{1,k}} \\
\frac{\partial L(θ_k)}{\partial θ_{2,k}} \\
\vdots \\
\frac{\partial L(θ_k)}{\partial θ_{M,k}}
\end{pmatrix}
$$

$$
θ_{k+1} = θ_k - η ∇L(θ_k)
$$
其中，$ η $ 是学习率，控制更新步长。

**Step4：迭代**：重复步骤2和3，直到损失函数收敛或达到预定的迭代次数。
$$
\begin{align*}
θ_0 & \rightarrow ∇L(θ_0) \\
θ_1 & = θ_0 - η ∇L(θ_0) \\
θ_1 & \rightarrow ∇L(θ_1) \\
θ_2 & = θ_1 - η ∇L(θ_1) \\
& \vdots \\
θ_k & \rightarrow ∇L(θ_k) \\
θ_{k+1} & = θ_k - η ∇L(θ_k) \\
& \vdots
\end{align*}
$$
假设参数总数为 $ M $，则 $ θ $ 是一个 $ M $ 维向量，梯度 $ ∇L $ 也是一个 $ M $ 维向量。通过不断迭代计算梯度并更新参数，我们最终可以找到使损失函数 $ L(θ) $ 最小的参数 $ θ^* $。

### 4、实现

我们先来生成数据，然后进行分段线性回归模型的训练，并在训练完成后展示拟合的回归线，并给出评测结果。

[实现](code/LinearRegression_3.py ':include :type=code ')

## 四、多项式线性回归

### 1、多项式回归原理

多项式回归是一种特殊的线性回归，它利用多项式特征来拟合数据中的非线性关系。尽管本质上仍属于线性模型，但通过引入自变量的多项式项，使其能够更好地描述非线性趋势。

多项式回归模型的一般形式为：

$$
y = \beta_0 + \beta_1 x + \beta_2 x^2 + \beta_3 x^3 + \ldots + \beta_d x^d + \epsilon
$$

其中：
- $ y $ 是响应变量（目标变量）。
- $ x $ 是自变量。
- $ \beta_0, \beta_1, \ldots, \beta_d $ 是模型的参数。
- $ \epsilon $ 是误差项。

在这个模型中，自变量 $ x $ 的多项式项（如 $ x^2, x^3, \ldots, x^d $）被当作新的特征引入，从而使模型能够捕捉数据中的非线性关系。

### 2、线性回归的基础

为了更好地理解多项式回归，我们先回顾一下线性回归。线性回归模型形式为：

$$
y = \beta_0 + \beta_1 x + \epsilon
$$

在线性回归中，我们假设响应变量 $ y $ 与自变量 $ x $ 之间的关系是线性的。线性回归模型使用最小二乘法来估计参数 $ \beta_0 $ 和 $ \beta_1 $，以使得预测值 $ \hat{y} $ 与实际值 $ y $ 之间的差异（即误差平方和）最小化。

### 3、扩展到多项式回归

多项式回归通过将原始特征 $ x $ 扩展为多项式特征来构建模型。例如，对于二次多项式回归，我们引入 $ x^2 $ 项，从而模型变为：

$$
y = \beta_0 + \beta_1 x + \beta_2 x^2 + \epsilon
$$

对于三次多项式回归，我们再引入 $ x^3 $ 项，模型变为：

$$
y = \beta_0 + \beta_1 x + \beta_2 x^2 + \beta_3 x^3 + \epsilon
$$

如此类推，可以引入更高次的多项式项来捕捉数据中的复杂非线性关系。尽管我们引入了多项式项，但这个模型仍然是线性回归，因为它在线性方程中只包含参数 $ \beta $ 的线性组合。

### 4、为什么使用多项式回归

**捕捉非线性关系**：在实际问题中，许多现象都表现出非线性特征。多项式回归通过引入自变量的多项式项，能够更好地拟合这些非线性关系。例如，经济学中的供需关系、工程学中的应力-应变关系、生物学中的生长曲线等，往往是非线性的。

**灵活性**：多项式回归通过增加多项式的次数（degree），可以变得非常灵活，能够拟合各种形式的非线性数据。这种灵活性使得多项式回归在处理复杂数据时具有显著优势。

**模型扩展**：多项式回归是线性回归的自然扩展，只需将原始特征扩展为多项式特征即可，无需引入复杂的非线性优化技术。这意味着在多项式回归中，我们仍然可以使用线性回归中常用的估计方法（如最小二乘法）来估计模型参数。

### 4、实现

以下是一个使用PyTorch实现多项式回归的代码示例。我们将使用鸢尾花数据集，并演示如何对数据进行多项式特征扩展，然后使用PyTorch训练一个多项式回归模型。

[实现](code/LinearRegression_4.py ':include :type=code ')

## 五、线性回归算法的评测

在使用线性回归算法进行建模时，评估模型的性能是至关重要的。常用的评估指标包括均方误差（Mean Squared Error，MSE）、均方根误差（Root Mean Squared Error，RMSE）和平均绝对误差（Mean Absolute Error，MAE）。这些指标可以帮助我们了解模型预测的准确程度，以及对异常值的敏感程度。

### 1、均方误差（MSE）

MSE是预测值与真实值之间差异的平方和的平均值，公式如下：
$$
MSE = \frac{1}{n} \sum_{i=1}^{n} (y^{(i)} - \hat{y}^{(i)})^2
$$

其中，$n$ 是样本数量，$y^{(i)}$是第 $i$个样本的真实值，$\hat{y}^{(i)}$ 是模型预测的值。

MSE越小，说明模型的拟合效果越好。

### 2、均方根误差（RMSE）

RMSE是MSE的平方根，公式如下：
$$
RMSE = \sqrt{MSE}
$$

RMSE在某种程度上弥补了MSE对量纲的依赖性，通常用于更直观地衡量模型的预测能力。

### 3、平均绝对误差（MAE）

MAE是预测值与真实值之间差异的绝对值的平均值，公式如下：
$$
MAE = \frac{1}{n} \sum_{i=1}^{n} |y^{(i)} - \hat{y}^{(i)}|
$$

MAE越小，说明模型对异常值的敏感性越低。

### 4、R Squared

R-squared$R^2$也称为决定系数（Coefficient of Determination），是用于衡量回归模型拟合优度的统计量。它表示模型解释目标变量总变异的比例。R-squared 的值介于 0 和 1 之间，值越接近 1，表示模型对数据的解释能力越强。R-squared 的定义公式如下：

$$
R^2 = 1 - \frac{SS_{\text{res}}}{SS_{\text{tot}}}
$$

$$
R^2 = 1 - \frac{\sum_{i=1}^{n} (y^{(i)} - \hat{y}^{(i)})^2}{\sum_{i=1}^{n} (y^{(i)} - \bar{y})^2}
$$

其中：$SS_{\text{res}}$是残差平方和（Sum of Squares of Residuals），即模型预测值与实际值之间的差异平方和。它表示模型未能解释的数据变异量。可以理解为模型预测得不好的地方有多大。

$$
SS_{\text{res}} = \sum_{i=1}^{n} (y^{(i)} - \hat{y}^{(i)})^2
$$

其中，$\hat{y}^{(i)}$ 是第 $i$ 个预测值。

$SS_{\text{tot}}$是总平方和（Total Sum of Squares），即实际值与实际均值之间的差异平方和。它表示数据的总变异量。可以理解为，如果我们只用一个平均值来预测所有数据，误差会有多大。
$$
SS_{\text{tot}} = \sum_{i=1}^{n} (y^{(i)} - \bar{y})^2
$$

其中，$y^{(i)}$是第 $i$ 个实际值，$\bar{y}$是目标变量的均值。

即R-squared 的定义公式也可为：
$$
R^2 = 1 - \frac{\sum_{i=1}^{n} (y^{(i)} - \hat{y}^{(i)})^2}{\sum_{i=1}^{n} (y^{(i)} - \bar{y})^2}
$$

R-squared 是 1 减去模型预测误差与总误差的比值。这个比值越小，R-squared 越接近 1，说明模型预测得越好。简单讲就是：

- 如果模型预测得很好，模型的预测误差$SS_{\text{res}}$就会很小，这样 R-squared 就会接近 1。
- 如果模型预测得不好，模型的预测误差$SS_{\text{res}}$会比较大，这样 R-squared 就会接近 0。
- **R² = 1**：表示模型能够完美预测数据，所有预测值都与实际值完全相同。
- **R² = 0**：表示模型无法解释数据的变异，预测值与实际值无关。

R-squared ($R^2$) 也可以通过均方误差 (MSE) 和总方差来表示。这种形式进一步简化了公式，并帮助我们理解 R-squared 的本质。我们可以将上面的公式转换成另一种表现形式：

总方差是总平方和除以样本数$n$：

$$
\text{Variance} = \frac{SS_{\text{tot}}}{n}
$$

均方误差 (MSE)是残差平方和除以样本数$n$：

$$
\text{MSE} = \frac{SS_{\text{res}}}{n}
$$

将这些代入 R-squared 的定义公式，可以得到 R-squared 的另一种形式：

$$
R^2 = 1 - \frac{\text{MSE}}{\text{Variance}}
$$
