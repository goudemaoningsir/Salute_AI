## 一、优化问题

在深度学习中，梯度下降是一种常用的优化方法，用于最小化损失函数并优化模型参数。然而，梯度下降也可能会遇到一些挑战，例如局部最小值问题，导致无法达到理想的损失水平。如果用图形表示，模型可以表示的函数集合形成的蓝色区域确实包含了一些损失较低的函数。然而，梯度下降算法可能无法找到这些损失较低的函数，它只是解决了优化问题并找到了某个参数 $ \theta^* $ 就停止了。

这就像是在大海中捞针，针确实在海里，但我们却无法将其捞起。当训练数据的损失不够低时，我们需要区分是模型偏差（model bias）还是优化问题。如果找不到损失低的函数，可能是因为模型的灵活性不够，即“海里没有针”，或者是模型的灵活性已经足够，但梯度下降优化不力，无法将针捞起来。

### 1、通过比较获得洞察

为了判断模型是否足够大，一个建议的方法是比较不同模型的表现。例如，我们可以比较20层和56层的残差网络（residual network）在测试集上的表现。根据2015年的一篇论文（链接：http://arxiv.org/abs/1512.03385），尽管56层网络在测试集上的损失比20层网络高，但这并不是过拟合（overfitting）。实际上，这表明56层网络的优化没有做好，因为如果优化成功，56层网络应该能够达到比20层网络更低的损失。

### 2、从浅层网络开始

面对一个新问题时，建议先尝试一些较小或较浅的网络，或者使用深度学习之外的方法，如线性模型或支持向量机。这些模型通常更容易优化，不太可能出现优化失败的问题。通过训练这些简单的模型，你可以先了解它们能达到的损失水平。

### 3、如果深层网络在训练数据上没有获得更小的损失，则存在优化问题

如果你发现深层模型与浅层模型相比，尽管具有更大的灵活性，但损失并没有更低，这表明优化存在问题，梯度下降可能不足够有效。在这种情况下，你需要寻找其他方法来改善优化过程。

例如，在观看人数预测的例子中，随着网络层数的增加，损失本应逐渐降低。但如果增加到5层时损失反而增大，这显然不是模型偏差问题，而是优化问题。

如果优化做得不好，我们将在下一节课中讨论如何解决这个问题。现在，你需要知道如何判断训练损失较大时是模型偏差还是优化问题。如果是模型偏差，可以通过增大模型来解决；如果是优化失败，那么需要参考后续课程来解决这个问题。

### 4、测试数据上的损失

当你通过努力使训练数据的损失变小后，接下来可以观察测试数据上的损失。如果测试数据的损失也很小，并且比强基线（strong baseline）还要小，那么就没有必要再做进一步的工作。但如果训练数据上的损失小而测试数据上的损失大，那么你可能真的遇到了过拟合问题。请注意，只有当训练损失小而测试损失大时，才称之为过拟合。许多同学在测试结果不佳时就认为是过拟合，但并非总是如此。

当你想要改进结果时，首先要检查训练数据上的损失。确保优化没有问题，模型足够大，然后才考虑是否是测试数据的问题。如果是训练损失小而测试损失大，那么可能是过拟合问题。

## 二、超参数

超参数是机器学习算法中的参数，但不同于模型参数（如权重和偏置），它们不是通过训练数据来学习的，而是在训练之前手动设置的。超参数的选择通常是基于经验、启发式方法或者试验来进行的。

### 1、常见的超参数

#### （1）学习率（Learning Rate）

学习率是控制模型在每次迭代中更新参数的步长大小。过小的学习率会导致收敛速度过慢，而过大的学习率可能会导致无法收敛。通常需要通过实验来调整学习率的大小。

#### （2）正则化参数（Regularization Parameter）

正则化参数用于控制模型的复杂度，防止过拟合。常见的正则化方法包括L1正则化和L2正则化，正则化参数的选择影响着模型的泛化能力。

#### （3）树的深度（Tree Depth）

对于决策树模型和基于树的集成模型（如随机森林、梯度提升树等），树的深度是一个重要的超参数，它控制着树的复杂度和拟合能力。

#### （4）窗口大小（Window Size）

在一些序列模型中（如循环神经网络、卷积神经网络等），窗口大小是一个重要的超参数，它影响着模型对序列数据的理解能力。

#### （5）批量大小（Batch Size）

批量大小是指在训练过程中每个小批量样本的数量，它影响着模型在训练过程中的稳定性和收敛速度。

### 2、超参数调优方法

#### （1）网格搜索（Grid Search）

网格搜索是一种常用的超参数调优方法，它通过在指定的超参数空间中进行穷举搜索来找到最优的超参数组合。

代码示例：

```python
from sklearn.model_selection import GridSearchCV

# 定义模型和超参数空间
rf = RandomForestClassifier()
param_grid = {
    'n_estimators': [50, 100, 200],
    'max_features': ['auto', 'sqrt', 'log2']
}

# 创建 GridSearchCV 对象
grid_search = GridSearchCV(estimator=rf, param_grid=param_grid, cv=5)
```

`GridSearchCV`的主要参数包括：

- `estimator`：要使用的模型，例如`RandomForestClassifier()`。
- `param_grid`：要搜索的参数网格，是一个字典或字典列表，键是参数名称，值是要尝试的参数值列表。
- `cv`：交叉验证的折数，用于评估每种参数组合的性能，默认为5折交叉验证。

在这个示例中，我们使用`RandomForestClassifier`作为模型，并定义了要搜索的`n_estimators`和`max_features`的参数网格。然后，我们创建了一个`GridSearchCV`对象，用于搜索最优的超参数组合。

#### （2）随机搜索（Random Search）

随机搜索是另一种超参数调优方法，与网格搜索不同的是，它是在指定的超参数空间中随机抽样来进行搜索，从而可以更快地找到较好的超参数组合。

#### （3）贝叶斯优化（Bayesian Optimization）

贝叶斯优化是一种基于贝叶斯统计模型的超参数调优方法，它通过不断地在超参数空间中探索，利用历史观察结果来指导下一次的超参数选择，从而高效地找到最优的超参数组合。

### 3、示例

我们将以随机森林模型为例，演示如何选择并调优模型中的两个超参数：树的数量（n_estimators）和最大特征数（max_features）。

步骤如下：

**Step1：准备数据集**：我们将使用scikit-learn中的鸢尾花（iris）数据集。

**Step2：划分数据集**：将数据集划分为训练集和测试集。

**Step3：定义模型**：使用随机森林模型，并设定初始超参数。

**Step4：选择超参数**：通过网格搜索(Grid Search)来选择最优的超参数组合。

**Step5：训练模型**：使用选择的超参数在训练集上训练模型。

**Step6：评估模型**：在测试集上评估模型性能。

实现如下：

[超参数调优](code/模型优化/超参数调优.py ':include :type=code ')

在这个例子中，我们使用网格搜索(Grid Search)来选择随机森林模型的最优超参数组合，通过在参数空间中进行穷举搜索，找到使模型性能最佳的超参数组合。然后使用最优超参数训练模型，并在测试集上评估模型的性能。

## 三、交叉验证

交叉验证（Cross-Validation, CV）是一种用于评估机器学习模型性能的技术，目的是通过分割数据集来验证模型的泛化能力，即模型在未见过的数据上的表现。交叉验证通过反复训练和测试模型，以减少过拟合和欠拟合的可能性，并获得更稳定和可靠的模型性能评估。

![img](https://raw.githubusercontent.com/goudemaoningsir/Salute_AI/main/img/31.jpg)

### 1、交叉验证的基本步骤

**Step1：数据分割**：将数据集分成多个子集（通常称为"折"或"folds"）。

**Step2：模型训练和验证**：在每次迭代中，使用一个子集作为验证集，剩余的子集作为训练集。训练模型并在验证集上评估模型性能。

**Step3：重复过程**：重复上述过程，直到每个子集都被用作一次验证集。

**Step4：性能平均**：计算每次迭代的评估指标（如准确率、均方误差等）的平均值，作为模型的总体性能。

交叉验证可以有效地评估模型的泛化能力，减少单次划分数据集带来的随机性。

**示例代码：**

[交叉验证](code/模型优化/交叉验证/交叉验证.py ':include :type=code ')

### 2、k折交叉验证（k-fold Cross-Validation）

k折交叉验证是最常用的一种交叉验证方法，其中"k"代表将数据集分成的子集数量。

#### （1）k折交叉验证的步骤

**Step1：数据分割**：将数据集随机分成k个等大小的子集。

**Step2：模型训练和验证**：

- 在第1次迭代中，使用第1个子集作为验证集，其余k-1个子集作为训练集。
- 在第2次迭代中，使用第2个子集作为验证集，其余k-1个子集作为训练集。
- 依此类推，直到每个子集都作为验证集一次。

**Step3：性能平均**：计算每次迭代的评估指标的平均值，作为模型的总体性能。

#### （2）k折交叉验证的优点

- **稳定性和可靠性**：相比于单次划分数据集，k折交叉验证更能提供稳定和可靠的性能评估。
- **充分利用数据**：每个数据点在不同的迭代中都作为训练集和验证集的一部分，充分利用了数据。

#### （3）k折交叉验证的缺点

- **计算成本**：对于大数据集和复杂模型，重复训练k次会增加计算成本。

#### （4）示例

[k折交叉验证](code/模型优化/交叉验证/k折交叉验证.py ':include :type=code ')

### 3、留一法交叉验证（Leave-One-Out Cross-Validation, LOOCV）

留一法交叉验证是k折交叉验证的极端情况，其中k等于数据集中样本的数量。

#### （1）留一法交叉验证的步骤

**Step1：数据分割**：将数据集分成n个子集，每个子集包含一个样本（n为数据集样本总数）。

**Step2：模型训练和验证**：

- 在第1次迭代中，使用第1个样本作为验证集，剩余n-1个样本作为训练集。
- 在第2次迭代中，使用第2个样本作为验证集，剩余n-1个样本作为训练集。
- 依此类推，直到每个样本都作为验证集一次。

**Step3：性能平均**：计算每次迭代的评估指标的平均值，作为模型的总体性能。

#### （2）留一法交叉验证的优点

- **充分利用数据**：每次迭代使用n-1个样本进行训练，保证了训练集的最大利用。
- **无偏估计**：适用于小数据集，可以获得无偏估计。

#### （3）留一法交叉验证的缺点

- **计算成本**：对大数据集而言，训练n次会带来很高的计算成本。
- **不稳定**：由于每次训练集只缺少一个样本，模型的评估结果可能会对个别数据点异常敏感。

#### （4）示例

[留一交叉验证](code/模型优化/交叉验证/留一交叉验证.py ':include :type=code ')

## 四、正则化

正则化是机器学习中用来防止过拟合的一种技术，通过在损失函数中引入惩罚项，限制模型的复杂度，增强模型的泛化能力。

### 1、正则化的基本概念

在机器学习中，我们通常通过最小化某个损失函数来训练模型。以线性回归为例，损失函数通常是均方误差（Mean Squared Error, MSE）：

$$
J(\mathbf{w}) = \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2
$$
其中，$\mathbf{w}$ 是模型的参数，$\mathbf{x}^{(i)}$ 是第 $i$ 个样本的输入特征，$y^{(i)}$ 是第 $i$ 个样本的实际输出，$m$ 是样本总数。

正则化通过在损失函数中添加正则化项来限制模型的复杂度，防止过拟合。正则化后的损失函数一般为：

$$
 J(\mathbf{w}) = \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2 + \lambda R(\mathbf{w})
$$
其中，$R(\mathbf{w})$ 是正则化项，$\lambda$ 是正则化参数，控制正则化项的权重。

### 2、L1正则化（Lasso）

Lasso（Least Absolute Shrinkage and Selection Operator）是一种线性回归模型，通过在损失函数中加入L1正则化项，既能限制模型复杂度，又能进行特征选择。

Lasso回归通过在损失函数中加入L1正则化项，有效地解决了普通最小二乘法回归中存在的多重共线性问题，限制了回归系数的大小，从而提高了模型的稳定性和泛化能力。更重要的是，Lasso回归能够将一些回归系数缩小为零，从而实现特征选择，使得模型更加简洁和可解释。在实际应用中，通常通过交叉验证选择最优的正则化参数，以获得最好的模型性能。

#### （1）背景与动机

在普通最小二乘法（OLS）回归中，目标是最小化均方误差（MSE）：

$$
J(\mathbf{w}) = \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2
$$
其中，$\mathbf{w}$ 是回归系数向量，$\mathbf{x}^{(i)}$ 是第 $i$ 个样本的特征向量，$y^{(i)}$ 是第 $i$ 个样本的目标值，$m$ 是样本总数。

当特征数量较多且存在多重共线性时，OLS回归的系数估计会变得不稳定，导致模型在测试集上的表现很差。Lasso通过引入L1正则化项，既能稳定系数估计，又能进行特征选择。

#### （2）Lasso回归的基本原理

Lasso回归通过在损失函数中加入L1正则化项（系数的绝对值之和），来限制模型的复杂度：

$$
J(\mathbf{w}) = \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2 + \lambda \sum_{j=1}^{n} |w_j|
$$
其中，$\lambda$ 是正则化参数，控制正则化项的权重。通过引入正则化项，Lasso可以将一些回归系数缩小为零，从而实现特征选择。

#### （3）Lasso回归的公式推导

Lasso回归的目标是最小化如下的损失函数：

$$
J(\mathbf{w}) = \frac{1}{2m} \left\| \mathbf{y} - \mathbf{X} \mathbf{w} \right\|^2 + \lambda \left\| \mathbf{w} \right\|_1
$$
其中：

- $\mathbf{X}$ 是 $m \times n$ 的设计矩阵
- $\mathbf{y}$ 是 $m \times 1$ 的目标向量
- $\mathbf{w}$ 是 $n \times 1$ 的回归系数向量
- $\left\| \mathbf{w} \right\|_1$ 表示 $\mathbf{w}$ 的L1范数，即所有系数绝对值之和

由于L1正则化项的不可导性，Lasso的解不能通过简单的解析方法得到，通常通过迭代算法如坐标轴下降（Coordinate Descent）来求解。

#### （4）坐标轴下降算法

坐标轴下降算法是一种常用的优化算法，适用于求解带有L1正则化的优化问题。其基本思想是固定其他变量，依次对每个变量进行优化，直至收敛。

算法步骤如下：

1. 初始化 $\mathbf{w}$。
2. 对每个系数 $w_j$，固定其他系数，最小化损失函数 $J(\mathbf{w})$。
3. 依次更新每个系数，直到损失函数收敛或达到最大迭代次数。

#### （5）正则化参数的选择

正则化参数 $\lambda$ 决定了正则化项的权重，进而影响模型的表现。通常通过交叉验证选择最优的 $\lambda$ 值。常见的方法是使用网格搜索（Grid Search）结合交叉验证来选择最佳参数。

#### （6）Lasso回归的优点和缺点

**优点**：

- 能够进行特征选择，使得模型更加简洁和可解释。
- 能够解决多重共线性问题。
- 通过引入L1正则化，限制了回归系数的大小，提高了模型的稳定性和泛化能力。

**缺点**：

- 在特征数量大于样本数量的情况下，Lasso可能会选择任意一个特征，导致结果不稳定。
- 对于高度相关的特征，Lasso倾向于选择其中一个，而忽略其他特征。

#### （7）示例

[L1](code/模型优化/交叉验证/留一交叉验证.py ':include :type=code ')

### 3、L2正则化（Ridge）

L2正则化，也称为岭回归（Ridge Regression），是一种用于解决多重共线性问题的线性回归技术。通过在普通最小二乘法（OLS）回归的损失函数中添加L2正则化项，岭回归能够限制回归系数的大小，从而提高模型的稳定性和泛化能力。

#### （1）背景与动机

在普通最小二乘法回归中，我们通过最小化均方误差（MSE）来求解回归系数：

$$
J(\mathbf{w}) = \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2
$$
其中，$\mathbf{w}$ 是回归系数向量，$\mathbf{x}^{(i)}$ 是第 $i$ 个样本的特征向量，$y^{(i)}$ 是第 $i$ 个样本的目标值，$m$ 是样本总数。

当特征之间存在高度相关性（多重共线性）时，普通最小二乘法回归的系数估计会变得不稳定，系数的方差会变得很大，从而导致模型在测试集上的表现很差。

#### （2）岭回归的基本原理

为了防止回归系数变得过大，岭回归在损失函数中加入了L2正则化项，即回归系数的平方和：

$$
J(\mathbf{w}) = \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2 + \lambda \sum_{j=1}^{n} w_j^2
$$
其中，$\lambda$ 是正则化参数，控制正则化项的权重。通过引入正则化项，岭回归能够限制回归系数的大小，从而提高模型的稳定性。

#### （3）岭回归的公式推导

岭回归的目标是最小化如下的损失函数：

$$
J(\mathbf{w}) = \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2 + \lambda \sum_{j=1}^{n} w_j^2
$$
我们可以将其写成矩阵形式：

$$
J(\mathbf{w}) = \frac{1}{2m} \left\| \mathbf{y} - \mathbf{X} \mathbf{w} \right\|^2 + \lambda \left\| \mathbf{w} \right\|^2
$$
其中，$\mathbf{X}$ 是 $m \times n$ 的设计矩阵，$\mathbf{y}$ 是 $m \times 1$ 的目标向量，$\mathbf{w}$ 是 $n \times 1$ 的回归系数向量。

通过对损失函数求导并设为零，我们可以得到回归系数的闭式解：

$$
\frac{\partial J(\mathbf{w})}{\partial \mathbf{w}} = -\frac{1}{m} \mathbf{X}^T (\mathbf{y} - \mathbf{X} \mathbf{w}) + 2\lambda \mathbf{w} = 0
$$
解得：

$$
\mathbf{w} = (\mathbf{X}^T \mathbf{X} + \lambda \mathbf{I})^{-1} \mathbf{X}^T \mathbf{y}
$$
其中，$\mathbf{I}$ 是 $n \times n$ 的单位矩阵。这个公式说明了岭回归在普通最小二乘法回归基础上添加了一个正则化项，使得系数估计更加稳定。

#### （4）正则化参数的选择

正则化参数 $\lambda$ 决定了正则化项的权重，进而影响模型的表现。通常通过交叉验证来选择最优的 $\lambda$ 值。常见的方法是使用网格搜索（Grid Search）结合交叉验证来选择最佳参数。

#### （5）岭回归的优点和缺点

**优点**：

- 通过引入L2正则化，限制了回归系数的大小，提高了模型的稳定性和泛化能力。
- 能够解决多重共线性问题。
- 在高维数据集上表现良好，能够处理特征数量多于样本数量的情况。

**缺点**：

- 无法进行特征选择，所有特征都会被保留下来。
- 模型的可解释性较差，特别是在高维数据集上。

### 4、弹性网（Elastic Net）

弹性网结合了 L1 和 L2 正则化，其正则化项为 L1 和 L2 正则化项的线性组合：

$$
J(\mathbf{w}) = \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2 + \lambda_1 \sum_{j=1}^{n} |w_j| + \lambda_2 \sum_{j=1}^{n} w_j^2
$$
其中，$\lambda_1 \sum_{j=1}^{n} |w_j|$ 是 L1 正则化项，$\lambda_2 \sum_{j=1}^{n} w_j^2$ 是 L2 正则化项。弹性网结合了 L1 正则化的特征选择能力和 L2 正则化的稳定性。

### 5、正则化项对优化问题的影响

正则化项的加入改变了模型参数的优化问题。以梯度下降为例，在每次更新参数时，正则化项会对参数施加额外的约束：

#### （1）L1 正则化

更新公式为：

$$
w_j := w_j - \eta \left( \frac{\partial}{\partial w_j} \left( \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2 \right) + \lambda \frac{\partial}{\partial w_j} \sum_{k=1}^{n} |w_k| \right)
$$
由于 L1 正则化项的导数为：

$$
\frac{\partial}{\partial w_j} \sum_{k=1}^{n} |w_k| = \text{sign}(w_j)
$$
因此，更新公式为：

$$
w_j := w_j - \eta \left( \frac{\partial}{\partial w_j} \left( \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2 \right) + \lambda \text{sign}(w_j) \right)
$$


#### （2）L2 正则化

更新公式为：

$$
w_j := w_j - \eta \left( \frac{\partial}{\partial w_j} \left( \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2 \right) + \lambda \frac{\partial}{\partial w_j} \sum_{k=1}^{n} w_k^2 \right)
$$
由于 L2 正则化项的导数为：

$$
\frac{\partial}{\partial w_j} \sum_{k=1}^{n} w_k^2 = 2w_j
$$
因此，更新公式为：

$$
w_j := w_j - \eta \left( \frac{\partial}{\partial w_j} \left( \frac{1}{2m} \sum_{i=1}^{m} \left( y^{(i)} - \mathbf{w}^T \mathbf{x}^{(i)} \right)^2 \right) + 2\lambda w_j \right) 
$$


### 6、总结

正则化通过在损失函数中添加惩罚项来限制模型的复杂度，提高模型的泛化能力。L1 正则化通过引入参数的绝对值之和来实现稀疏性，有助于特征选择；L2 正则化通过引入参数的平方和来均匀缩小所有参数的值，提高模型的稳定性；弹性网结合了 L1 和 L2 正则化，兼具特征选择和稳定性的优点。了解这些正则化方法及其公式推导，有助于在实际应用中选择合适的正则化技术，构建更好的机器学习模型。